<form method="post" action="{{ path('app_podcasts_delete', {'id': podcast.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ podcast.id) }}">
    <button class="btn">Delete</button>
</form>
