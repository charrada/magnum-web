<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h6 class="card-subtitle mb-2 text-muted">{{ history.time }}</h6>
    <h5 class="card-title">{{ history.activity }}</h5>
    <p class="card-text">{{ history.description }}</p>
  </div>
</div>
